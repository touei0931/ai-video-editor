# Phase 0 では疎通確認のみで、重い依存はまだ入れない。
# Phase 1 以降で必要になるものをコメントで残しておく。
#
# ⚠ ライセンス上「最初から避けるもの」（§13.1）:
#   - ultralytics（AGPL-3.0）→ MediaPipe を使う
#   - x264 / x265 を含む ffmpeg ビルド（GPL）→ LGPL ビルドを vendoring する
#   - libfdk-aac（非フリー）→ ネイティブ AAC を使う

# --- Phase 0 T4 で導入済み ---
faster-whisper          # ASR。CUDA が使えなければ自動で CPU に落ちる
                        # NVIDIA GPU を使う場合は requirements-cuda.txt も入れること
                        # （約800MBあるので既定には入れない。無くても CPU で動く）

# --- Phase 1 ---
# pywhispercpp          # Mac 側 ASR（CoreML/Metal）。T5 で対応
# silero-vad            # 無音検出
# numpy

# --- Phase 2 ---
# librosa               # 韻律（RMS）
# pyworld               # F0（DIO/StoneMask）
# sudachipy             # 形態素解析（BudouX が使えない場合のフォールバック）

# --- 文字起こし: Qwen3-ASR（日本語の会話で最も誤りが少ない）---
# Windows は本家 qwen-asr（torch が要る。CUDA 版 torch を先に入れること:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128 ）
# Mac は mlx-qwen3-asr（Apple の GPU。torch 不要）。[aligner] は語の時刻用の分かち書き（nagisa）
qwen-asr ; sys_platform != "darwin"
mlx-qwen3-asr[aligner]>=0.4.0 ; sys_platform == "darwin"

# --- Phase 3（③ズーム・画角の自動化）---
sherpa-onnx             # 話者埋め込み（声で「誰が喋っているか」を見分ける / Apache-2.0）
                        # torch は使わない（配布物が 2GB 太り、Mac 版に載らない）
                        # モデル本体は scripts/fetch_models.py で取得する
mediapipe               # 顔検出（BlazeFace / Apache-2.0）
                        # モデル本体は scripts/fetch_models.py で取得する
                        # ⚠ Ultralytics YOLO は AGPL なので使わない
# scenedetect           # シーンカット検出
