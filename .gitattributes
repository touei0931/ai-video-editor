# 動画・音声はリポジトリに置かない方針のため、Git LFS は使わない。
# テスト素材は scripts/make_test_media.py で毎回生成する（samples/README.md）。

# 改行コードは LF に統一（Windows で開発し macOS でビルドするため）
* text=auto eol=lf
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf
